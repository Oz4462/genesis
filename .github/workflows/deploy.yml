# Generated by crew. Auto-deploy: preview on PR, production on main.
# Runs only after CI passes (the gate stack is the safety net).
name: Deploy
on:
  push:
    branches: [main]
  pull_request:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build image
        run: docker build -t app:${{ github.sha }} .
      # add your registry push / deploy here
