# PR-Style Plan: Bring Todos to End (Genesis Full Close + Rekursive Selbstverbesserung)

**Branch context (from prior):** feat/app-integration-phase0-2 (local, 76 ahead). No push until owner gate. Suite baseline: ~1200+ passed (slices green post prior closes).  
**Date/Context:** 2026-06-24. Harness: grok-laptop (structured-cycle + self-apply + WORKFLOW + personas). Genesis project root: /home/genesis/genesis.  
**Trigger:** User: "bring the todos to end" + "full energy full power full input" + "rekursive selbstverbesserung" (structured loop self-applied to harness while closing genesis todos).

## Master Objective
Close the frontier and high-priority honest gaps documented in the PAUSE section + Return Gate (WORK_QUEUE.md, docs/BUILD_LOG.md, verification/CodeKnowledge.md) so there are **0 high open issues** in WORK_QUEUE.md. Simultaneously self-apply the structured loop + harness improvements (self-apply/SKILL.md, WORKFLOW.md, structured-cycle/SKILL.md) to strengthen future "finish todos" campaigns. Demonstrate recursion by using a newly improved harness pattern to close at least one genesis item. All changes follow 4 LINSEN + Return Gate + full DoD (from grok-laptop/templates/coding/DefinitionOfDone.md) + structured-cycle discipline. Memory (WQ + BUILD_LOG + CK + WQ PAUSE) fully updated with cites/proofs. No over-claims; honest first-stone gaps preserved where real data/ingest remains out of scope.

## Success Criteria (Measurable)
- **0 high open issues** in WORK_QUEUE.md frontier/PAUSE (Step7-9 notes + Return Gate gaps either closed or explicitly marked "first-stone honest, next phase" with evidence). Updated PAUSE/resume section.
- **Memory updated**: WQ.md (new close marker + frontier cleared), docs/BUILD_LOG.md (new "RETURN GATE CLOSE" append + severity table updates), verification/CodeKnowledge.md (frontier snapshot + closes), HORIZON.md (status table), verification/verification-log.md (if appendable) or equivalent.
- **Demonstrated closures**: 3-5 genesis items + 2 harness items. Each with:
  - Exact 4L writeup (L1-L4).
  - Execution proof (pytest slice + smoke cmd + wiring grep).
  - No breakage (full relevant test green, ruff clean).
- **Harness stronger for future closes**: 1-2 improvements that directly aid "finish todos" (e.g. automated Return Gate + memory sync enforcement, recursion demo protocol).
- **Recursion demonstrated**: One genesis close (e.g. reviewed_failure_modes) executed using the new harness pattern introduced in H1 (mini Return Gate checklist + memory-append step applied by implementer subagent).
- **Exit**: strict-reviewer (or equivalent) reports **0 open issues** after final review round. Full DoD satisfied (testing, wiring proofs, docs as-you-go, theoretical/algorithmic notes where relevant, error handling, no hidden TODOs). Suite slices remain green.
- Reviewer can confirm: "All high items closed or honestly documented; harness improved; everything runs and is understandable."

## Research Summary (from required reads + own verification)
- **WORK_QUEUE.md (PAUSE + frontier)**: MAX AGENTS loop closed cleanly 2026-06-21. Solid: physics validators (~42 RECIPES, L3 seams to pipeline/HORIZON), HORIZON cert attach (small E2E in lumencrucible + typed RunState), CAD TEIL2 + electronics polish (named consts), consumers surface support added. Honest gaps: full rich δ+ E2E reality ingest, reviewed_failure_modes thin, detect expr/domain, inventor-γ+ bridge, Step7-9 (dfm/flight/robot/circuit depth) noted open. WQ Step7-9 "remains noted open". Memory sync done that cycle. Resume = re-read PAUSE + verif/CK + current high.
- **docs/BUILD_LOG.md (Return Gate)**: Ruthless consolidated review (strict-reviewer + thorough). Severity table: CRITICAL dummies (some since closed in γ+ derive real from spec.quantities — architect/lumen now use derive_goal_from_spec). HIGH: reviewed collection thin + skeleton notes (conductor: "skeleton: conductor", lumen "skeleton: no full measurement"), dynamic (now typed in state), omega partial, inverse contract. MEDIUM: seams detect (left/right bare qid check + _guess_domain), inventor bridge, HORIZON doc drift ("✓ bewiesen" vs first-stone), consumers (partially addressed), test coverage (test_phase_epsilon hardcoded), over-claim in logs/tmp. Gaps list prioritized 1-10. Later appends show E2E cert chain tests + consumers + γ+ dummy fix + typed fields CLOSED. Verdict: first-stone achieved; FAIL full claims.
- **verification/CodeKnowledge.md**: Mirrors Return Gate + subsequent fixes (γ+ dummies removed; state fields added; E2E tests; consumers surface). Remaining: #3 reviewed full, #4 detect, #5 real E2E ingest, doc sync, test strengthen. Selfkontrolle + 4L explicit. Recent: γ+ fix details with greps/proofs.
- **self-apply/SKILL.md**: Full structured loop on harness itself (research/plan/impl/vibe-verify/review). Treat grok-laptop as "project". Vibe-verify + real task execution to prove improvement. No "schöner text" only; must change behavior + compare + document in DECISIONS/ logs.
- **WORKFLOW.md**: Mandatory phases (Intake→Research→Plan→Implement→Vibe-Verify+Exec→Review→Close). DoD mandatory (incl. theoretical foundations: algos/complexity, discrete math, paradigms, concurrency, numerics). 0 open from reviewer + DoD = exit only. Rubber duck auto, hermes-head for multi-harness, worktree for risk. Continuous doc + wiring proofs.
- **structured-cycle/SKILL.md**: Orchestrator spawns personas (thorough-researcher, loop-planner, careful-implementer, strict-reviewer, rubber-duck). Never does work itself. Exit ONLY on reviewer 0 issues + DoD. Hello-vibe-validation as quality bar (full CK, exec proofs, wiring). Anti-hallu: main reads artifacts itself post-subagent. Vibe-verify mandatory for code.
- **Supporting (HORIZON.md, 4_LINSEN_PRINZIP.md, docs/)**: Phases status table marks most δ+/γ+/ε/ζ/Ω "first-stone (skeleton)". Honest gaps section cites Return Gate. 4L (L1 Truth/Provenance, L2 Drift, L3 Naht/Seams, L4 Realisierbarkeit/Verif) mandatory per item. DoD template requires explicit wiring verification, execution proof, continuous docs, no creep.
- Current src state (reads/greps): Dummies largely removed (architect uses real derive; state has typed coverage/reality/delta; omega _has includes them). Remaining demo skeletons in conductor/lumencrucible for δ+ reviewed/measurement (honest because no rich prod claims/measurements yet). seams.py detect uses strict qid-in + simple _guess. Consumers + tests advanced but coverage gaps remain for auto detect.
- Prior closes respected (no re-open): γ+ real derive, typed fields, E2E tests, consumers surface, CAD electronics named consts, physics depth.

No high open "bugs" (code green); these are honest elaboration gaps for "full" vs first-stone.

## Prioritized Work Packages (3-5 Genesis + 2 Harness)

**Priority rule:** Harness first (bootstrap recursion + stronger loop for genesis closes). Then genesis high (reviewed/δ+ ingest/detect from Return Gate) + doc/memory + 1 WQ Step7-adjacent. Smallest-safe changes only. Guarded where possible. 4L + Return Gate per package.

### Harness Improvements (Self-Apply — Execute Full structured-cycle on grok-laptop as project)
**H1: Structured Return Gate + Memory Sync Enforcement in structured-cycle/SKILL.md (core loop strengthening for future "finish todos")**  
- **Exact files:** /home/genesis/grok-laptop/skills/structured-cycle/SKILL.md (main), optionally cross-ref in WORKFLOW.md + self-apply/SKILL.md + templates/coding/DefinitionOfDone.md (add "Return Gate + memory" item).  
- **Smallest change idea:** Add a new mandatory "Phase 5.5 / Return Gate + Memory Close Step" (after Vibe-Verify, before Review): "For todo-closing or gap campaigns: (a) run explicit mini Return Gate (re-read key wires/gaps from CK/BUILD + severity table update), (b) prove 4L on changes, (c) append structured close entry to genesis BUILD_LOG.md + verification/CodeKnowledge.md + WQ.md (using cites/lines/proofs), (d) demonstrate recursion if self-apply context. Add 'rekursive selbstverbesserung demo' rule. Update orchestrator exit check to require evidence of this step (read logs yourself). Keep hello-vibe bar." ( ~15-25 lines additive, no behavior change to existing non-todo paths).  
- **Verification (test cmd, smoke, wiring proof):** 
  - `cd /home/genesis/grok-laptop; cat skills/structured-cycle/SKILL.md | grep -A 30 "Return Gate + Memory" ` (wiring).
  - Run a mini structured cycle on a trivial task (or reuse hello-vibe-validation experiment) via persona; verify orchestrator instructions now force memory append + Return Gate (read produced verification-log or CK copy).
  - Smoke: python -c "import yaml; ... " or direct (no code exec needed beyond doc; treat as config). Post-change: spawn a careful-implementer subagent on a genesis gap using the NEW rule (recursion).
  - ruff/equivalent lint on any supporting py if touched (none expected).
- **4L:**
  - L1: Grounded in current structured-cycle text (phases 1-7, "read artifacts yourself", "DoD", "verification-log.md", "exit only on 0 issues"). Prior Return Gate cites in genesis CK/BUILD.
  - L2: No drift from existing (additive phase; mirrors "vibe-verify + execution round (MANDATORY)", "update verification-log"). Improves exactly the "finish todos" weakness identified in PAUSE/Return Gate.
  - L3: Naht to WORKFLOW DoD, self-apply rules, genesis memory (BUILD/CK/WQ), 4_LINSEN_PRINZIP. Orchestrator handoff now explicitly includes genesis artifacts.
  - L4: Verifiable (grep + re-run cycle), realizable (text instruction change), preserves hello-vibe bar + anti-hallu reads.
- **Harness impact:** Future loops auto enforce the exact discipline used to close this campaign (Return Gate + memory + recursion demo).

**H2: Codify Rekursive Selbstverbesserung + Todo-Close PAUSE/Resume Protocol in WORKFLOW.md (and cross in loop-planner persona)**  
- **Exact files:** /home/genesis/grok-laptop/WORKFLOW.md (main addition), /home/genesis/grok-laptop/personas/loop-planner.toml (update instructions for campaigns).  
- **Smallest change idea:** In WORKFLOW.md "For Vibe Coder..." + "Overrides" or new "Advanced Patterns" section: "Rekursive Selbstverbesserung (self-apply during genesis todo close): 1. Identify harness gap while closing genesis item. 2. Spawn self-apply on that harness slice (treat grok-laptop as target). 3. Prove improvement by re-running the genesis close item with new pattern. Add PAUSE protocol mirroring genesis WQ: 'Save: WQ PAUSE section + CK/BUILD + verif-log. Resume: re-read PAUSE + Return Gate + frontier. 0 schedulers. MAX AGENTS + 4L + Return Gate on resume.' Update loop-planner.toml instructions to require 'include 1-2 harness packages + recursion demo + memory update' for todo-close plans." (smallest targeted paragraphs + 1 list).  
- **Verification:** Grep for new sections + "rekursive". Re-run a plan generation (loop-planner persona) and confirm it produces recursion + memory sections. Compare pre/post behavior on a trivial planning task. Update DECISIONS.md or grok-laptop/verification/ with "before/after" note. Full cycle on this H2 itself.
- **4L:** 
  - L1: From WORKFLOW text (phases, DoD, hermes, rubber duck), self-apply/SKILL, genesis WQ PAUSE.
  - L2: Additive; directly addresses "rekursive" user request + PAUSE resume guidance.
  - L3: Seams to structured-cycle (orchestrator), personas (loop-planner), genesis WQ/4L/practice.
  - L4: Immediately testable (plan output changes), no code risk.
- **Harness impact:** Makes "self-apply while closing" and clean pause/resume first-class, stronger for repeated "bring todos to end".

### Genesis Closes (3-5; use improved harness from H1/H2 where possible)
**G1: Full reviewed_failure_modes collection (no early-break, no dummy fallback, remove skeleton notes) — HIGH Return Gate gap #3 (conductor + lumen)**  
- **Exact files:** /home/genesis/genesis/src/gen/agents/conductor.py (core _enrich_delta_plus reviewed loop ~372-410), /home/genesis/genesis/src/gen/grenzverschiebung/lumencrucible.py (similar reviewed + delta attach ~430+), tests/test_lumencrucible.py + tests/test_phase_delta_plus*.py or test_pipeline.py (add/enhance assert for |reviewed| >1 or full collection), docs/BUILD_LOG.md + verification/CodeKnowledge.md + WORK_QUEUE.md (append closes).  
- **Smallest change idea:** In conductor reviewed loop: collect ALL REFUTED (keep current for loop), remove `if not reviewed` fallback dummy creation + "skeleton" note; always attach even empty (honest). Mirror in lumen (guarded). Update comments to "full collection from skeptic/N-Judge/claims (no early break per Return Gate)". Add 1-2 boundary tests for multi-REFUTED case (use existing test claim factories). No behavior change on happy paths.  
- **Verification (test cmd, smoke, wiring proof):** 
  - `cd /home/genesis/genesis; PYTHONPATH=src python -m pytest tests/test_lumencrucible.py tests/test_phase_delta_plus*.py -q -k "delta or coverage or reviewed or lumen" --tb=line`
  - Smoke: `PYTHONPATH=src python -c "
from gen.agents.conductor import Conductor; from gen.core.state import RunState, Question; ... (construct with >=2 claims, one REFUTED); c=Conductor(); c._enrich...; print(len(rs.coverage_certificate.reviewed_failure_modes if ...)); assert 'skeleton' not in str(...)
" ` (wiring + removal proof).
  - Grep post: `grep -n "skeleton: conductor\|reviewed: list = \[\]" src/gen/agents/conductor.py src/gen/grenzverschiebung/lumencrucible.py` (0 hits).
  - ruff: `cd /home/genesis/genesis; ruff format --check src/gen/agents/conductor.py src/gen/grenzverschiebung/lumencrucible.py; ruff check ...`
- **4L:** L1 (from CK/BUILD exact lines + conductor code); L2 (removes thin + skeleton exactly as criticized; matches "enrich from N-Judge"); L3 (seams conductor→state.coverage→omega gate_omega + lumen + tests); L4 (tests + smoke prove collection; realizable; preserves honest empty).
- **Recursion tie-in:** Close this using the new H1 "Return Gate + Memory" step (implementer performs mini re-read of CK severity + appends structured entry).

**G2: Detect expr support + domain taxonomy improvement (MEDIUM Return Gate gap #4)**  
- **Exact files:** /home/genesis/genesis/src/gen/seams.py (detect_cross_domain_seams ~410-440 + _guess_domain ~450-461 + _quantity_map helper), tests/test_phase_epsilon.py (add real detect call + roundtrip gate_epsilon), tests/test_pipeline.py (add .seam asserts), pipeline.py if needed for call site, HORIZON.md + logs.  
- **Smallest change idea:** In constraint loop: keep qid check but add simple expr handling note + fallback (if left/right not direct qid, try split on ops or use existing evaluate_seam_expression context); enhance _guess_domain with bom/components + more terms (from physics taxo). Add comment "addresses Return Gate expr support (constraints left/right as qty exprs)". 1 test exercising detect on constraint with cross + gate roundtrip.  
- **Verification:** `PYTHONPATH=src python -m pytest tests/test_phase_epsilon.py tests/test_pipeline.py -q -k "epsilon or seam or detect" --tb=line`. Smoke: `PYTHONPATH=src python -c "
from gen.seams import detect_cross_domain_seams; from gen.core.state import ...; s=...; seams=detect...; print([ (s.left_domain, s.relation) for s in seams]); assert any...
" ` (wiring). Grep for improved _guess + constraint handling. ruff. Post: test_phase_epsilon no longer "0 coverage".
- **4L:** L1 (from seams docstring + BUILD/CK cites); L2 (improves exact "bare" + "_guess string only" without changing contract); L3 (seams→architect/pipeline/omega/gate_epsilon + tests); L4 (new test + smoke; gates unchanged).

**G3: Real E2E δ+ reality ingest beyond 9.81 hardcoded demo (HIGH Return Gate gap #5)**  
- **Exact files:** /home/genesis/genesis/src/gen/agents/conductor.py (_enrich ~330-370 Falsif/Meas construction), /home/genesis/genesis/src/gen/simulation/runner.py (δ+ sections ~684+), /home/genesis/genesis/src/gen/grenzverschiebung/lumencrucible.py (delta attach), reality.py (if small enhance), tests (enhance or tmp smoke), logs.  
- **Smallest change idea:** In conductor: if state has recent physics/simulation output or claim-backed value, prefer it for predicted_value/Measurement (e.g. pull from claims or runner report if attached); else honest demo + note "real ingest from runner/sim when available (first-stone)". Mirror guarded in lumen/runner. Remove/harden "demo values" comment. Conditional only (no breakage).  
- **Verification:** Targeted pytest slices + `PYTHONPATH=src python tmp_..._verify.py` (or new smoke exercising runner delta path). Grep for "9.81" only in test/demo comments. Wiring: grep calls from runner/cond to reality.evaluate + state attach. ruff. Prove one path uses non-hardcoded if possible (or honest fallback).
- **4L:** L1 (reality.py evaluate + prior CK "beyond 9.81"); L2 (addresses "demo" exactly); L3 (conductor/runner/lumen → reality → state → omega); L4 (smoke + existing gate tests).

**G4: Doc sync + honest status (MEDIUM #6 + HORIZON/WQ drift)**  
- **Exact files:** /home/genesis/genesis/docs/HORIZON.md (table 100-110 + Honest Gaps section), /home/genesis/genesis/WORK_QUEUE.md (PAUSE frontier + new close section at end), /home/genesis/genesis/docs/BUILD_LOG.md (new append), /home/genesis/genesis/verification/CodeKnowledge.md (frontier snapshot + closes list update), possibly DOC_CODE_DRIFT.md.  
- **Smallest change idea:** Table: change "✓ bewiesen" for δ+/γ+/ε/ζ/Ω to "first-stone (skeleton + wires live; rich E2E pending Return Gate G3/G1)". Update Honest Gaps list to post-close status (reference G1-3). WQ: append "CLOSED [date] via loop-close-plan: G1/G2/G3 + H1/H2. Frontier now: X. Memory: cites." BUILD/CK: structured close table update + "0 high after this". No code.
- **Verification:** `grep -n "first-stone\|✓ bewiesen\|delta_plus\|reviewed_failure_modes" docs/HORIZON.md WORK_QUEUE.md` (post vs pre). Read updated sections. No tests (docs).
- **4L:** L1 (from HORIZON table + prior CK quotes); L2 (removes documented drift); L3 (docs seam to src + CK/BUILD/WQ); L4 (verifiable by grep/read).

**G5 (optional 5th to reach "full energy"): Advance one WQ Step7/electronics or omega subgate (e.g. even subgate guards in omega.py)**  
- **Exact files:** /home/genesis/genesis/src/gen/omega.py (subgate section ~432-440 + gate_omega reviewed checks), related tests, logs. (Or electronics.py if remaining magic; but prior polish closed most.)  
- **Smallest:** Replace bare try:pass for gamma+ subgate with consistent guarded pattern matching ε/ζ; add explicit note "Return Gate uniform subgates". 1 test assert.  
- **Verification:** pytest -k omega + smoke gate call. 4L as above.

## Parallelization (subagents, worktrees)
- **Harness (H1/H2):** Full structured-cycle on grok-laptop (loop-planner spawns thorough-researcher → plan self → careful on SKILL/WORKFLOW → vibe-verify exec on trivial task → strict). Single worktree ok (docs/config).
- **Genesis research:** hermes-head + multiple subagents (thorough-researcher on conductor/seams/reality in parallel; different personas for cross-review).
- **Implement:** Separate worktrees for:
  - worktree-genesis-core (conductor + lumen + reality + seams + tests)
  - worktree-docs-memory (HORIZON + WQ + BUILD + CK)
  - worktree-harness (H1/H2)
- Subagents: careful-implementer (per WP, injected with new H1 pattern for recursion demo on G1), strict-reviewer per package, visual not needed.
- Main (loop-planner/head): reads all post-subagent, runs verif cmds, updates todo_write.
- Risky (any state mutation): worktree + isolated pytest.
- MAX AGENTS where possible (as prior successful pattern).

## Recursion Demonstration (Explicit)
1. Execute H1 first (structured-cycle on grok-laptop → improved SKILL with Return Gate + Memory step + "use for recursion demo").
2. For G1 (reviewed): careful-implementer prompt explicitly: "Apply the new Return Gate + Memory step from updated structured-cycle/SKILL.md: perform mini re-read of CK/BUILD severity, make change, append structured entry to BUILD_LOG/CK/WQ yourself, prove 4L. This is the rekursive selbstverbesserung demo."
3. Post-G1: re-execute a small slice of the loop using the improved orchestrator instruction (or manual equivalent) to show the new pattern closed the item stronger/faster.
4. In final plan/CK: "Used H1 pattern to close G1 (evidence: implement log + appended sections citing the new phase)."

## Risk Mitigation
- **Overclaim / doc drift (high from prior Return Gate):** Strictest honest language only ("first-stone", "closes reviewed collection", "enables richer ingest"). Every claim cites lines/greps. 4L L1-L2 mandatory.
- **Breakage (wiring/tests):** Guarded additive changes only. Read-before-edit. Pytest slice + full relevant before/after + ruff. Worktree isolation.
- **Scope creep:** Stick exactly to listed gaps from Return Gate + WQ PAUSE. No new features. Plan is reviewable.
- **Memory inconsistency:** All closes require explicit append to BUILD/CK/WQ in the package (enforced by H1).
- **Harness self-apply risk:** Treat as real project (per self-apply/SKILL); full vibe-verify + exec on trivial task + compare pre/post.
- **0 issues exit:** Final strict-reviewer round + DoD load + count open issues = 0 required. If >0, iterate implement.
- **Scheduler/owner:** Confirm 0 schedulers; all local; no push.

## DoD Checklist Application (per grok-laptop/templates/coding/DefinitionOfDone.md + WORKFLOW)
For **every** package (H/G):
- Functionality: smallest change + happy/edge (empty reviewed, no-spec, etc.) proven by smoke.
- Testing: min smoke + targeted pytest (or doc read for pure H). Adversarial for reviewed/detect.
- Logging/Error: existing guarded + new honest notes preserved/added.
- Documentation: as-you-go (code comments + CK/BUILD/WQ appends); final CodeKnowledge update.
- Scope: matches this plan exactly.
- Bad inputs: tests cover missing claims/spec.
- Data contracts: RunState fields, DomainSeam, CoverageCertificate etc. unchanged or enhanced consistently.
- Formatting/Linting: ruff (Python) on all touched src. Prettier if any JS (unlikely).
- Wiring proofs: greps + python -c imports/calls + execution.
- Theoretical/Algo: note complexity (linear collection in reviewed; O(n) detect) where relevant in CK.
- Full DoD loaded by strict-reviewer + head confirmation.
- "Everything must actually run" + "user understands" via CK + final summary.

## Order of Operations
1. **Intake + todo_write** (this plan + master todos).
2. **Harness first (self-apply full cycle):** H1 (structured-cycle) + H2 (WORKFLOW). Use structured-cycle orchestrator. Vibe-verify + exec proof on harness change. Update DECISIONS.md/grok-laptop logs.
3. **Apply recursion:** For G1, instruct implementer with H1 rule; execute + memory append using new pattern.
4. **Parallel genesis closes:** G2/G3 (seams + ingest) research/impl in parallel worktrees/subagents. G4 (docs) anytime (low risk). G5 if capacity.
5. **Per-package:** Research (read/grep) → 4L plan → smallest impl (read first) → exec/smoke/ruff/grep proof → append memory → vibe-verify.
6. **Head reads + cross-verif:** Main reads every artifact post-subagent. Run global smoke (relevant pytest -q + genesis CLI or tmp_*.py if applicable).
7. **Final review round:** strict-reviewer on all artifacts + verification-log updates + full DoD + count 0 open. Iterate if needed.
8. **Memory finalize + WQ close:** Update WQ PAUSE/frontier, BUILD_LOG Return Gate close section, CK snapshot. Mark "0 high".
9. **Exit + report:** 0 issues confirmed. Summary + links to proofs.

## Exit Criteria (Reviewer 0 Issues)
- strict-reviewer report: 0 open issues (severity table empty or all CLOSED).
- All packages have evidence of execution (logs/cmd output), 4L, wiring greps, ruff clean, DoD items checked.
- WQ frontier section: "0 high open (closed via loop-close-plan G1-5 + H1-2; honest first-stone remaining: [list if any])".
- Full suite slice green + no regression.
- Recursion demo documented with before/after or "used new H1 step".
- Head (loop-planner) personally read key files + ran verif cmds.
- Plan.md + all memory updated.

**Next step recommendation (after this plan):** Spawn structured-cycle on H1 ("improve structured-cycle with Return Gate + memory enforcement for todo closes") using this plan as input. Then resume with G1 using the output.

---
**Todos Breakdown (for tracking via todo_write / orchestrator):**
- [ ] H1 research + plan slice
- [ ] H1 implement + vibe-verify + memory append on harness
- [ ] H2 research + implement + compare
- [ ] G1 research (conductor/lumen reviewed paths)
- [ ] G1 impl (full collection) + use H1 pattern (recursion demo) + appends
- [ ] G2 seams detect + tests
- [ ] G3 δ+ ingest enhancement
- [ ] G4 doc sync (HORIZON/WQ/BUILD/CK)
- [ ] G5 (if) omega subgate
- [ ] Cross verif smokes + greps (head)
- [ ] Final strict-review (0 issues)
- [ ] WQ/BUILD/CK finalize + PAUSE close marker
- [ ] Full DoD + exit

All packages must produce their own 4L + proof section in the updated logs.

**References (absolute paths from reads):**
- WORK_QUEUE.md:280 (PAUSE end + frontier)
- docs/BUILD_LOG.md:69 (Gaps list 1-5+), 3-67 (Return Gate table)
- verification/CodeKnowledge.md:69 (same gaps post some closes)
- grok-laptop/skills/structured-cycle/SKILL.md:60-90 (phases + exit)
- grok-laptop/WORKFLOW.md:40-70 (DoD + verif)
- grok-laptop/skills/self-apply/SKILL.md:20-30 (rules)
- genesis/docs/HORIZON.md:103-130 (table + gaps)
- src/gen/agents/conductor.py:372+, lumencrucible.py:430+, seams.py:420+, omega.py:310+, state.py:1330+ (current state)

This plan is reviewable end-to-end. Execute with full power + 4L at every step.